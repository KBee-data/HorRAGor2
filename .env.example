# Copier ce fichier en .env et remplir les valeurs (le .env n'est JAMAIS commite).

# --- Supabase ---
SUPABASE_URL=
SUPABASE_KEY=

# --- API ---
API_HOST=127.0.0.1
API_PORT=8000
API_BASE_URL=http://127.0.0.1:8000

# --- Modeles (a decider en equipe) ---
LLM_MODEL=a-definir
EMBEDDING_MODEL=a-definir
